# Keeping node_modules out matters most: copying a host-built tree into the
# image would ship platform-specific binaries and defeat `npm ci`.
node_modules/
dist/
coverage/

.env
.env.*
!.env.example

.git/
.gitignore

*.log
.DS_Store
