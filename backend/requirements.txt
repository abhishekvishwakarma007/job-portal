fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
pydantic==2.10.4
pydantic-settings==2.7.0
alembic==1.14.0
# bcrypt directly rather than via passlib: passlib is unmaintained and its
# 1.7.4 release crashes reading bcrypt's version on bcrypt >= 4.1.
bcrypt==4.2.1
pyjwt==2.10.1
# Backs Pydantic's EmailStr, which validates addresses at the API boundary.
email-validator==2.2.0
